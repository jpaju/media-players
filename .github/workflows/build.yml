name: Build

on:
  push:
  pull_request:
  workflow_dispatch:
  release:
    types: [published]

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  build-firmware:
    name: Build Firmware
    uses: esphome/workflows/.github/workflows/build.yml@2025.10.0
    with:
      files: |
        m5stack/m5stack-atom-echo.factory.yaml
        m5stack/m5stack-atom-speaker-kit.factory.yaml
        m5stack/m5stack-atom-echo.yaml
        m5stack/m5stack-atom-speaker-kit.yaml
        onju-voice/onju-voice.yaml
        onju-voice/onju-voice.factory.yaml
        raspiaudio/raspiaudio-muse-luxe.yaml
        raspiaudio/raspiaudio-muse-proto.yaml
        raspiaudio/raspiaudio-muse-luxe.factory.yaml
        raspiaudio/raspiaudio-muse-proto.factory.yaml
      esphome-version: 2025.9.3
      release-summary: ${{ github.event_name == 'release' && github.event.release.body || '' }}
      release-url: ${{ github.event_name == 'release' && github.event.release.html_url || '' }}
      release-version: ${{ github.event_name == 'release' && github.event.release.tag_name || '' }}
